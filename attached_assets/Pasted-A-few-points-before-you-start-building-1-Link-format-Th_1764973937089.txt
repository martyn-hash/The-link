A few points before you start building:

1. **Link format**

   * The `/queries/respond/{token}` format is correct.
   * Please just double-check that in production the email actually uses the `https://flow.growth.accountants/queries/respond/{token}` URL (not the internal `render:///…` or `*.replit.dev` URL).

2. **Email borders – your current plan is too light**

   * Updating “email design for all clients” is overkill. I only want you to update the **query email template** you just added (the one that sends the list of bookkeeping queries).
   * CSS-only borders are the problem. Outlook in particular needs the old-school HTML attributes as well.

   Please update the query table markup in the email template to something along these lines:

   ```html
   <table
     border="1"
     cellpadding="0"
     cellspacing="0"
     bordercolor="#d0d7de"
     style="border-collapse:collapse; mso-table-lspace:0pt; mso-table-rspace:0pt; width:100%;"
   >
     <tr>
       <th
         align="left"
         style="border:1px solid #d0d7de; padding:6px; font-weight:bold; font-size:13px;"
       >
         Date
       </th>
       <th
         align="left"
         style="border:1px solid #d0d7de; padding:6px; font-weight:bold; font-size:13px;"
       >
         Description
       </th>
       <th
         align="right"
         style="border:1px solid #d0d7de; padding:6px; font-weight:bold; font-size:13px;"
       >
         Amount
       </th>
       <th
         align="left"
         style="border:1px solid #d0d7de; padding:6px; font-weight:bold; font-size:13px;"
       >
         Our Query
       </th>
     </tr>
     <!-- data rows use the same td styles -->
   </table>
   ```

   Key requirements:

   * `border="1"` and `bordercolor` on the `<table>` itself.
   * `cellpadding="0"` and `cellspacing="0"` on the table.
   * `mso-table-lspace:0pt; mso-table-rspace:0pt; border-collapse:collapse;` in the table `style`.
   * Every `<th>` and `<td>` in that query table must have an explicit `style="border:1px solid #d0d7de; padding:6px;"` (or similar padding) so borders render in Outlook.

3. **Scope of changes**

   * Do **not** touch other email templates yet.
   * Only change the **bookkeeping query email** that lists the transactions and “our query” column.
   * Once you’ve made the change, show me the final HTML snippet for the table you’re generating in that email so I can sanity-check it.

Please implement these table changes and keep everything else in the email template as it is.
